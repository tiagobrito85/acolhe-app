services:
  - type: web
    name: acolhe-backend
    runtime: python
    pythonVersion: "3.11"
    rootDir: acolhe-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
